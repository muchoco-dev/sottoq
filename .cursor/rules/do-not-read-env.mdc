---
description: Do not read dotenv secret files
alwaysApply: true
---

# Do not read .env

Never read, print, grep, or cat `.env` or `.env.*` files, except `.env.example`. Use `.env.example` for key names only. Do not put dotenv secret values in chat, plans, or commits.
